{
  "entities": {
    "surveys": {
      "title": "Survey",
      "description": "Client survey response",
      "type": "object",
      "properties": {
        "clientName": { "type": "string" },
        "answers": { "type": "object" },
        "completedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["clientName", "answers", "completedAt"]
    }
  },
  "firestore": {
    "surveys": {
      "schema": "surveys",
      "description": "Collection of client survey responses"
    }
  }
}
